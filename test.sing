LIB "symbolicPowers.lib";
// LIB "random.lib";

ring R = 0,(x,y,z),dp;
// ideal I = randomid(maxideal(2),3,5);
// I;
// symbolicPowerofPrimeIdeal(ideal(var(1),var(2)),3);
// join( I, I );
ideal I = y^2-x*z,x^2*y-z^2,x^3-y*z;
symbolicPowerofIdeal(I,2);

